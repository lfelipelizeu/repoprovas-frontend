import styled from 'styled-components';
import palette from '../styles/Colors';

const MainContainer = styled.main`
    width: 100vw;
    height: 100vh;
    color: ${palette.ivory};
    background-color: ${palette.blackCoffee};
    overflow: hidden;
`;

export default MainContainer;